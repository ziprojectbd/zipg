{
  "name": "zi-pay",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "concurrently -n frontend,backend -c cyan,magenta \"pnpm --dir frontend dev\" \"pnpm --dir backend dev\"",
    "build": "pnpm --dir frontend build && pnpm --dir backend build",
    "lint": "pnpm --dir frontend lint && pnpm --dir backend lint",
    "typecheck": "pnpm --dir frontend typecheck && pnpm --dir backend typecheck",
    "start": "node backend/dist/server.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@base-ui/react": "^1.7.0",
    "class-variance-authority": "^0.7.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  }
}
